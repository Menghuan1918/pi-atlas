{
  "name": "@pi-atlas/ask",
  "version": "0.2.0",
  "description": "ask_user extension — ask the user questions (select/input) and block for answers, with goal-auto timeout capping.",
  "license": "MIT",
  "type": "module",
  "files": [
    "extensions"
  ],
  "keywords": [
    "pi-package",
    "pi",
    "coding-agent",
    "extensions"
  ],
  "pi": {
    "extensions": [
      "extensions/*/index.ts"
    ]
  },
  "dependencies": {
    "@pi-atlas/shared": "0.2.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
