/**
 * Shared types for the Target extension.
 *
 * Re-exported from `pi-atlas-shared/target-state.js` (the canonical model
 * lives in the shared package so extensions outside `base` — e.g. the ask
 * package — can read target state without importing this code).
 */
export * from "pi-atlas-shared/target-state.js";
